import 'package:flutter/material.dart';

class CourseView extends StatelessWidget {
  static const String name = 'course_view';
  const CourseView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Course view'),
    );
  }
}